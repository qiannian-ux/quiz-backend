/**
 * 流量主广告位 ID
 * ------------------------------------------------------------
 * 在【微信公众平台 → 流量主 → 广告位】分别创建 3 种广告位后，
 * 把下面占位值换成后台给的 adUnitId（三种类型 ID 不能混用）。
 *
 * 抖音端不用这套：抖音走穿山甲，API 不同（见 utils/ads.js 底部 TODO）。
 */
export const AD_UNIT = {
  banner: 'adunit-xxxxxxxxxxxx',        // 结果页底部条幅
  interstitial: 'adunit-yyyyyyyyyyyy',  // 答题完成 → 结果页 插屏
  rewarded: 'adunit-dabbff5702ad830d'  // 激励视频：看广告·激励作者（我的页自愿激励）
}

/**
 * 「看广告解锁深度解读」开关 —— 2026-09-20 起作废，保留常量防旧引用报错。
 *
 * 为什么作废：用户明确不做付费墙 / 广告墙。初级解析 + 高级解析两档**全部免费**，
 * 结果页只保留自愿打赏（请作者喝奶茶）。卡住核心内容换广告，
 * 既踩微信诱导付费红线，也和「治愈系」口碑相悖。
 *
 * 保留这里的常量只是为了别的页面/分支不会 import 报错，
 * 新代码不要再用它做解锁判定。
 */
export const ENABLE_AD_UNLOCK = false

